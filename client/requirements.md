## Packages
framer-motion | Page transitions and complex animations
lucide-react | Icons for the UI (already in base, but listing for completeness)
react-hook-form | Form handling
@hookform/resolvers | Zod resolvers for forms
zod | Schema validation
clsx | Class name utility
tailwind-merge | Class name utility

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
